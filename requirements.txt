flask
gunicorn
requests
python-dotenv
# Prefer ddgs for search (Python 3.9: use ddgs==9.0.x; 3.10+: any ddgs). Fallback: duckduckgo-search.
ddgs
duckduckgo-search
openai
instructor
pydantic
# sentence-transformers removed — RAG now uses OpenAI embeddings API (zero local memory).
# spaCy removed — blis C compilation times out on Render. Entity extraction falls back to regex.
beautifulsoup4
numpy
