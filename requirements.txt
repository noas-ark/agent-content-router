flask
gunicorn
requests
python-dotenv
# Prefer ddgs for search (Python 3.9: use ddgs==9.0.x; 3.10+: any ddgs). Fallback: duckduckgo-search.
ddgs
duckduckgo-search
openai
instructor
pydantic
# spaCy removed — requires blis C compilation which fails on some cloud build envs.
# Entity extraction falls back to regex (see _get_spacy_nlp in app.py).
sentence-transformers
beautifulsoup4
numpy
